name: build
permissions:
  contents: read
on:
  push:
    branches:
    - main
  workflow_dispatch:
jobs:
  build_windows:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v4
    - uses: "./.github/actions/build-windows"
      with:
        GOARCH: amd64
